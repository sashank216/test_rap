_schema-version: "3.2"
ID: travustvstravelproc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travustvstravelproc-destination-content
  type: com.sap.application.content
  requires:
  - name: travustvstravelproc-destination-service
    parameters:
      content-target: true
  - name: travustvstravelproc-repo-host
    parameters:
      service-key:
        name: travustvstravelproc-repo-host-key
  - name: travustvstravelproc-uaa
    parameters:
      service-key:
        name: travustvstravelproc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travustvstravelproc_html_repo_host
          ServiceInstanceName: travustvstravelproc-html5-srv
          ServiceKeyName: travustvstravelproc-repo-host-key
          sap.cloud.service: travustvstravelproc
        - Authentication: OAuth2UserTokenExchange
          Name: travustvstravelproc_uaa
          ServiceInstanceName: travustvstravelproc-xsuaa-srv
          ServiceKeyName: travustvstravelproc-uaa-key
          sap.cloud.service: travustvstravelproc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travustvstravelproc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travustvstravelproc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travustvstravelproc.zip
      name: travustvstravelproc
      target-path: resources/
- name: travustvstravelproc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travustvstravelproc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travustvstravelproc-destination-service
    service-plan: lite
- name: travustvstravelproc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travustvstravelproc-xsuaa-srv
    service-plan: application
- name: travustvstravelproc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travustvstravelproc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
